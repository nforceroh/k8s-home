apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: test-cert
spec:
  dnsNames:
    - test.k3s.nf.lab
  issuerRef:
    kind: ClusterIssuer
    name: nf-lab
  secretName: test-cert-tls
  subject:
    organizations:
      - "Nforcer"
  usages:
    - server auth