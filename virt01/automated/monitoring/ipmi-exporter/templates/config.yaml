apiVersion: v1
kind: Secret
metadata:
  name: ipmi-exporter-config
  namespace: monitoring
stringData:
  config.yml: |
    modules:
      fw-mgmt.v101.nf.lab:
        driver: LAN
        pass: monitor
        user: monitor
      virt01m.v101.nf.lab:
        driver: LAN
        pass: monitor
        user: monitor
