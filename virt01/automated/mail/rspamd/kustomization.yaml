apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mail

labels:
  - pairs:
      app: rspamd

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - serviceaccount.yaml
  - unbound-config.yaml
#   - backup-schedule.yaml
