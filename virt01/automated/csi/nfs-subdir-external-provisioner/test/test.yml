apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opnsense-poc-claim
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: opnsense-nfs
  resources:
    requests:
      storage: 10Gi
