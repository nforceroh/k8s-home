apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app: frigate

namespace: camera

resources:
  - deployment.yaml
  - service.yaml
  - serviceaccount.yaml
  - ingress.yaml
#  - pvc.yaml
  - secrets.yaml
  - config-cm.yaml
  - alerting.yaml

generatorOptions:
  disableNameSuffixHash: true



