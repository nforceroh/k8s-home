apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app: postgresql

namespace: databases

resources:
  - cluster.yaml

generatorOptions:
  disableNameSuffixHash: true