apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app: postgresql

namespace: databases

resources:
  - pg-db-app-sealed.yaml
  - pg-db-superuser-sealed.yaml
  - cluster.yaml
#   - backup-schedule.yaml

generatorOptions:
  disableNameSuffixHash: true