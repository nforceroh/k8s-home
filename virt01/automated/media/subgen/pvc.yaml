apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: subgen-models-pvc
  namespace: media
  annotations:
    argocd.argoproj.io/sync-options: Prune=false,Delete=false
    helm.sh/resource-policy: keep  
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: openebs-zfspv-lz4
  resources:
    requests:
      storage: 20Gi
  volumeMode: Filesystem
