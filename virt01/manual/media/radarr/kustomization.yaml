apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app: radarr

namespace: media

resources:
  - deployment.yaml
#  - pvc.yaml
  - service.yaml
  - ingress.yaml
  - serviceaccount.yaml
#   - backup-schedule.yaml
