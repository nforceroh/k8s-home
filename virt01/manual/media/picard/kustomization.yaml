apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: media

labels:
  - pairs:
      app: picard

resources:
  - pvc.yaml
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
