apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dovecot-pvc-test
  namespace: mail
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
