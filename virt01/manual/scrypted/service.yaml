---
apiVersion: v1
kind: Service
metadata:
  name: scrypted-svc
  labels:
    app: scrypted
  annotations:    
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
    external-dns.alpha.kubernetes.io/hostname: scrypted.nforcer.com
    external-dns.alpha.kubernetes.io/ttl: "120"    
spec:
  ports:
    - port: 10443
      protocol: TCP
      name: https
  selector:
    app: scrypted
