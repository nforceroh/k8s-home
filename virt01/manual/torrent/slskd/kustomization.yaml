apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app: slskd

namespace: torrent

resources:
  - slskd-config-sealed.yaml
  - deployment.yaml
  - service.yaml
  - serviceaccount.yaml
  - pvc.yaml
  - ingress.yaml
