apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app: droppedneedle

namespace: torrent

resources:
  - deployment.yaml
  - service.yaml
  - serviceaccount.yaml
  - pvc.yaml
  - ingress.yaml
