apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app: subarr

namespace: torrent

resources:
  - subarr-config-sealed.yaml
  - deployment.yaml
  - pvc.yaml
  - service.yaml
  - ingress.yaml
  - serviceaccount.yaml
