apiVersion: v1
kind: Namespace
metadata:
  name: torrent
  labels:
    # Opts this namespace in to the pod-gateway mutating webhook; pods still
    # need the setGateway="true" annotation since webhook.gatewayDefault=false.
    routed-gateway: "true"
