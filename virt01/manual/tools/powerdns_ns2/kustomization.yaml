apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tools

labels:
  - pairs:
      app: powerdns-ns2

resources:
  - powerdns-ns2-config-sealed.yaml
  - deployment.yaml
  - serviceaccount.yaml
  - services.yaml
  
generatorOptions:
  disableNameSuffixHash: true