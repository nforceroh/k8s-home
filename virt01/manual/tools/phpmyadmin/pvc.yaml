apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: phpmyadmin-pvc
  namespace: tools
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  volumeMode: Filesystem
