apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tools

labels:
  - pairs:
      app: poweradmin

resources:
  - poweradmin-config-sealed.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - serviceaccount.yaml

generatorOptions:
  disableNameSuffixHash: true