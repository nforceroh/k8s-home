apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kms-pvc
  namespace: tools
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  volumeMode: Filesystem
